stages:
  - module-pipelines

kluster-kubernetes:
  stage: module-pipelines
  trigger:
    include:
      - local: /yandex-k8s-cluster/.gitlab-ci.yml
    strategy: depend
  only:
    changes:
      - yandex-k8s-cluster/**/*     
